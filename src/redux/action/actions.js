import { COURSE_DETAIL } from "./actiontypes";

export function getCoursesDetail () {
    return async function(){
        try {
            
        } catch (error) {
            console.log(error);
        }
    }
}