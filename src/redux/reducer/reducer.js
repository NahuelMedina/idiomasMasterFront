import { COURSE_DETAIL } from "../action/actiontypes";

let initialState= {

}

export const reducer = (state=initialState, {type, payload})=>{
    switch (key) {
        case COURSE_DETAIL:
            
            break;
    
        default:
            break;
    }
}